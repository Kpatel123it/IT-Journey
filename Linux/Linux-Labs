# Linux Practice Labs

## Lab 1

Objective

Practice file management commands.

Commands Used

mkdir LinuxPractice

cd LinuxPractice

touch notes.txt

cp notes.txt backup.txt

mv backup.txt notes_backup.txt

rm notes_backup.txt

find . -name "*.txt"

Skills Learned

- Creating directories
- Creating files
- Copying files
- Renaming files
# Lab 2 - Linux Networking Basics

## Objective

Learn how to inspect network configuration and test connectivity.

## Commands Used

```bash
hostname
hostname -I
ip addr
ip route
ping google.com
ping 8.8.8.8
nslookup google.com
traceroute google.com
```

## Skills Practiced

- Viewing hostname
- Finding IP address
- Viewing routing table
- Testing Internet connectivity
- Testing DNS resolution
- Tracing packet routes

---

# Lab 3 - Linux File Permissions

## Objective

Learn Linux file permissions using symbolic and numeric methods.

## Commands Used

```bash
mkdir PermissionLab
cd PermissionLab

touch notes.txt
touch script.sh
touch secret.txt

ls -l

chmod +x script.sh
chmod -w notes.txt
chmod 777 secret.txt
chmod 755 script.sh
chmod 644 notes.txt
chmod 600 secret.txt

ls -l
stat notes.txt
```

## Skills Practiced

- Viewing permissions
- Changing permissions
- Numeric permissions
- Symbolic permissions
- Viewing file metadata

---

## Key Learning

Linux permissions are essential for protecting files and controlling access to system resources.
- Deleting files
- Searching files
# Lab 4 – Linux Users and Groups

## Objective

Learn how Linux manages user accounts, groups, and user-related system files.

## Commands Used

```bash
whoami
who
id
groups
cat /etc/passwd
cat /etc/group
echo $USER
echo $HOME
echo $SHELL
```

## Skills Practiced

- Identifying the current user
- Viewing logged-in users
- Checking UID and GID
- Viewing group memberships
- Reading user account information
- Reading group information
- Viewing environment variables

## Result

Successfully explored Linux user management and understood how user and group information is stored on the system.
# Lab 5 – Linux User Management

## Objective

Practice creating, modifying, and deleting Linux user accounts.

---

## Commands Used

```bash
whoami
id
groups
sudo adduser testuser
id testuser
groups testuser
sudo usermod -aG sudo testuser
groups testuser
sudo deluser testuser
```

---

## Skills Practiced

- Viewing current user information
- Creating a new user
- Viewing user details
- Viewing user groups
- Adding a user to the sudo group
- Removing a temporary user

---

## Result

Successfully created a temporary user account, verified user information, assigned administrative privileges, and removed the user after completing the lab.
# Lab 6 – Linux File Ownership

## Objective

Practice viewing and changing Linux file ownership and group ownership.

---

## Commands Used

```bash
pwd
mkdir ownership-lab
cd ownership-lab
touch file1.txt
touch file2.txt
touch file3.txt
ls -l
whoami
sudo chown testuser file1.txt
sudo chgrp sudo file1.txt
sudo chown testuser:sudo file2.txt
ls -l
```

---

## Skills Practiced

- Viewing ownership
- Creating files
- Changing owner
- Changing group
- Verifying ownership

---

## Result

Successfully viewed file ownership, changed file owners, modified group ownership, and verified changes using `ls -l`.
# Lab 7 – Linux Process Management

## Objective

Learn how to view, monitor, and terminate Linux processes.

---

## Commands Used

```bash
ps
ps aux
top
htop
sleep 300 &
jobs
ps aux | grep sleep
kill PID
jobs
```

---

## Skills Practiced

- Viewing running processes
- Monitoring CPU and memory
- Running background jobs
- Finding Process IDs
- Stopping processes

---

## Result

Successfully viewed and managed Linux processes using multiple command-line tools.
# Lab 8 – Ubuntu Package Management

## Objective

Learn how to manage software packages using APT.

---

## Commands Used

```bash
sudo apt update
sudo apt upgrade
apt search tree
apt show tree
sudo apt install tree
tree
sudo apt remove tree
sudo apt autoremove
apt list --installed
```

---

## Skills Practiced

- Updating repositories
- Upgrading packages
- Installing software
- Searching packages
- Removing software
- Cleaning unused packages

---

## Result

Successfully managed Ubuntu software packages using the APT package manager.
# Lab 9 – Linux Services & System Logs

## Objective

Practice monitoring Linux services and reading system logs.

---

## Commands Used

```bash
systemctl list-units --type=service --state=running
systemctl --failed
journalctl -n 20
journalctl -b
journalctl | grep error
```

---

## Skills Practiced

- Viewing running services
- Viewing failed services
- Reading system logs
- Searching log entries

---

## Result

Successfully explored Linux services and reviewed system logs using systemctl and journalctl.
# Lab 10 – SSH Installation and Verification

## Objective

Install and verify the SSH service on Ubuntu.

---

## Commands Used

```bash
which ssh
systemctl status ssh
sudo apt update
sudo apt install openssh-server
systemctl status ssh
systemctl is-enabled ssh
```

---

## Skills Practiced

- Checking SSH installation
- Installing OpenSSH Server
- Managing Linux services
- Verifying automatic startup

---

## Result

Successfully installed OpenSSH Server and confirmed the SSH service is active and enabled.
# Lab 11 – Linux File Compression and Archiving

## Objective

Practice creating archives and compressing files.

---

## Commands

```bash
mkdir Day14Lab
cd Day14Lab

touch file1.txt
touch file2.txt
touch notes.txt

tar -cvf backup.tar *.txt
tar -tvf backup.tar

mkdir Extracted
tar -xvf backup.tar -C Extracted

gzip backup.tar
gunzip backup.tar.gz

zip backup.zip *.txt
unzip backup.zip
```

---

## Skills Practiced

- Archive creation
- Archive extraction
- Compression
- Decompression
- ZIP management

---

## Result

Successfully created, compressed, extracted, and verified Linux archive files.
# Lab 12 – Linux Special Permissions

## Objective

Practice Linux permission management and explore SUID, SGID, and Sticky Bit.

---

## Commands

```bash
mkdir PermissionsLab
cd PermissionsLab

touch file1 file2 file3

ls -l

chmod 777 file1
chmod 755 file2
chmod 644 file3

ls -l

ls -l /usr/bin/passwd
ls -ld /tmp
```

---

## Skills Practiced

- Viewing permissions
- Modifying permissions
- Understanding SUID
- Understanding SGID
- Understanding Sticky Bit

---

## Result

Successfully reviewed Linux permissions and understood the purpose of special permissions.
# Lab 13 – Linux Disk Management

## Objective

Practice monitoring filesystem usage and identifying storage consumption.

---

## Commands

```bash
df -h

du -sh ~

du -sh *

lsblk

mount

sudo apt clean

sudo apt autoclean

sudo apt autoremove
```

---

## Skills Practiced

- Checking filesystem usage
- Measuring directory size
- Viewing block devices
- Viewing mount points
- Cleaning package cache

---

## Result

Successfully monitored disk usage and safely removed unnecessary package cache.
# Lab 14 – File Search & Log Files

## Objective

Practice searching for files, viewing file contents, and exploring Linux log files.

---

## Commands

```bash
find . -name "*.txt"

which bash

whereis bash

head /etc/passwd

tail /etc/passwd

less /etc/passwd

ls /var/log

tail /var/log/syslog
```

---

## Skills Practiced

- Searching files
- Finding executables
- Reading large files
- Exploring Linux logs

---

## Result

Successfully located files and examined Linux log files for troubleshooting.
# Lab 15 – Linux Text Processing

## Objective

Practice searching, sorting, counting, and extracting data using Linux text-processing commands.

---

## Commands

```bash
grep

wc

sort

uniq

cut
```

---

## Practical Commands

```bash
grep John users.txt

sort users.txt

sort users.txt | uniq

wc users.txt

wc -l users.txt

cut -d ":" -f1 /etc/passwd
```

---

## Skills Practiced

- Searching text
- Counting lines
- Sorting files
- Removing duplicates
- Extracting fields

---

## Result

Successfully processed text files using standard Linux administration tools.
# Lab 16 – Linux System Monitoring

## Objective

Practice monitoring Linux system performance and identifying resource usage.

## Commands

```bash
uptime

free -h

top

ps

ps aux

df -h

du -sh ~
```

## Skills Practiced

- Checking uptime
- Monitoring RAM
- Viewing CPU usage
- Reviewing running processes
- Checking disk usage

## Result

Successfully monitored Linux system resources using standard administration tools.
# Lab 17 – Linux Pipes and Redirection

## Objective

Practice combining Linux commands using pipes and redirection.

## Commands

```bash
cat notes.txt

cat notes.txt | wc -l

cat notes.txt | grep Linux

ls > files.txt

cat files.txt

ls | tee output.txt

ps aux | grep bash

df -h | grep "/"
```

## Skills Practiced

- Pipes
- Output redirection
- Append redirection
- tee command
- Filtering command output

## Result

Successfully combined Linux commands to process information efficiently.
Day 22  Linux Commands

```bash
systemctl status ssh

systemctl list-units --type=service

systemctl status NetworkManager
```

Observed:

- Active services
- Service status
- Loaded state
- Running state
Day 23 # Linux Commands

```bash
journalctl

journalctl -n 20

journalctl --since today

journalctl -u ssh

journalctl -f

cat /var/log/auth.log
```

Observed:

- Recent log entries
- SSH service logs
- Boot messages
- Authentication logs
- Real-time log monitoring

---# Skills Learned

- Understanding Linux logs
- Using journalctl
- Reading service logs
- Viewing recent logs
- Investigating SSH issues using logs
- Using logs instead of guessing
